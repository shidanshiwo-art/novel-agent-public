# 项目开发规则

## 设计原则

- 不要过度设计。以当前明确需求为边界，优先采用简单、直接且易于维护的实现；不得为尚未出现的场景预先引入抽象层、扩展点或复杂架构。
- 不要过度解释为什么不做某项内容，说明应聚焦已采用的方案和实际结果。
- 不要解释已经决定删除的内容；删除后不再保留相关背景、理由或替代方案说明，除非用户明确要求。
- 计划、任务拆解和实施步骤统一使用中文。
- 开始实现前先确定当前需求所必需的设计细节；存在会影响实现结果的未确定事项时，应先确认，不得自行补充需求或扩大设计范围。

## HTTP 接口约束

- 项目新增或调整的 HTTP 接口只允许使用 `GET` 和 `POST`；禁止新增 `PUT`、`PATCH`。
- 查询使用 `GET`；新增、修改、删除等写操作统一使用 `POST`，通过路径和请求体区分动作。已有历史兼容接口后续迁移时遵循该规则。

## 测试代码位置

- 所有测试代码统一放在 `novel-agent-app/src/test` 下。
- `novel-agent-domain`、`novel-agent-infrastructure`、`novel-agent-trigger` 等业务模块中不得新增 `src/test` 测试目录。
- 领域层和基础设施层的测试仍可使用原有 Java package，以便直接测试对应模块的公开类，但物理文件必须位于 `novel-agent-app/src/test` 下。
- 新增测试时，应按原 package 在 `novel-agent-app/src/test/java` 下建立目录。
- 测试固件等仅测试使用的资源放在 `novel-agent-app/src/test/resources` 下；环境配置文件不适用此规则。

## app 资源结构

- 公共配置放在 `novel-agent-app/src/main/resources/application.yml`。
- 环境配置分别放在 `application-dev.yml`、`application-prod.yml`、`application-stress.yml` 和 `application-test.yml`。
- 日志配置统一使用 `logback-spring.xml`。
- MyBatis 相关配置和 Mapper 分别放在 `mybatis/config` 与 `mybatis/mapper`；尚未使用的目录也应保留该结构。
- 数据库 SQL 脚本统一放在 `docs/sql`，不放入 `novel-agent-app/src/main/resources`。
- SQL 脚本迁移到 `docs/sql` 后，应用不再通过 Spring Boot 自动初始化数据库；需要建表时手动执行对应脚本。

## 测试验证

- 修改或新增测试后，只运行与本次改动直接相关的定向测试，不得默认执行 `mvn -pl novel-agent-app -am test` 等全量测试命令。
- Maven 定向测试应使用 `-Dtest=<测试类或测试方法>` 限定范围；多模块构建时可配合
  `-Dsurefire.failIfNoSpecifiedTests=false`，避免未包含目标测试的模块报错。
- 只有用户明确要求全量验证时，才允许运行全量测试。
- 编写测试时必须打印关键执行过程和测试结果，便于直接检查实际行为；不能只写简单的 Assert 断言。断言仍应用于自动判定测试是否通过。
- 需要连接数据库的测试统一使用 `novel-agent-app/src/main/resources/application.yml` 及对应环境配置中的数据源连接信息；不得在测试代码或测试资源中重复配置、硬编码数据库地址、账号或密码。
- `.gitignore` 只用于防止其他模块的测试文件被提交，不能替代测试目录归属规则；如果发现其他模块下存在测试，应将其迁移到 `novel-agent-app/src/test`。
- 仓储、Mapper 和结构化上下文加载以真实 MySQL 集成测试为主：测试应写入可清理的隔离数据并调用真实 DAO/Repository，不使用全 Mock 测试重复验证字段映射。
- Mock 仅用于真实外部依赖不可控或需要精确模拟异常分支的场景；能够使用本地真实数据库验证的持久化逻辑不得用 Mock 替代。

## 前端响应式布局

- 工作台、编辑器和管理页面的主容器默认使用可用空间的完整宽度，并设置 `min-width: 0`；
- 不得用固定像素宽度或固定 `max-width` 限制整个页面。小说正文等确有阅读行长要求的内容区域可以单独限制宽度。
- 多栏布局使用 `grid`/`flex` 配合 `minmax()`、`clamp()` 或弹性比例分配空间；固定像素仅用于图标、按钮等尺寸明确的小型控件，不用于主内容栏。
- 新增或调整页面布局后，至少检查常规桌面、窄屏桌面和移动端三个宽度，确保内容能随容器伸缩、断点降级合理且没有横向滚动条。

## Element Plus 组件使用规范

- 项目使用 Vue 3 + Element Plus，并已配置 Element Plus 自动导入；新页面优先使用 Element Plus 已有组件，不新增其他 UI 组件库。
- 输入框、按钮、树、表单、Select、Dialog、Drawer、Tag、Tooltip、Popconfirm、Empty、Loading 等交互组件不得重复手写基础实现。
- 自定义 CSS 只负责页面布局、间距和少量视觉调整，不重新实现组件库已有行为。
- 现有 `OutlineView.vue` 中的手写树节点、原生搜索框和旧章纲平铺结构不作为新实现参考。
- 不得为了视觉效果重新封装或手写 Input、Textarea、Button、Select、Collapse、Dialog、Drawer、Tag 等 Element Plus 已有基础控件；业务组件只有在存在明确复用的业务行为时才允许抽取，不能只为包装样式创建组件。
- 不得通过大量 `:deep(.el-xxx)`、内部 DOM 选择器或复杂 CSS 将 Element Plus 基础控件改造成另一套自定义组件体系。允许调整边框、背景、间距、字体等轻量视觉，但不得依赖 Element Plus 私有 DOM 结构实现核心交互和布局。
- 原生 `<button>`、`<input>`、`<textarea>` 不得替代 Element Plus 已有同类交互组件；纯展示元素和没有组件库对应行为的结构除外。
- 页面重构后必须删除已不再被模板使用的旧 class、旧布局 CSS 和旧组件代码；不得长期保留多代 UI 实现和无效样式。

## 创作编辑界面

- 小说设定、章纲、正文等需要连续思考的创作页面，应优先设计为聚焦的编辑画布，不得将所有字段做成同时展开的厚重表单卡片墙。
- 标题或名称类短字段优先使用内联、低边界的编辑样式；长文本默认单栏全宽，避免并列放置两个主要写作文本框。
- 可增删的规则、层级、标签等结构化内容应使用紧凑条目列表，支持明确的新增和删除操作，不得伪装成大面积文本域或暴露技术数据格式。
- 一次只需完成一个创作主题时，使用分区导航、折叠或逐步引导降低同时出现的输入量；说明文字按需简短呈现，不能挤占主要写作空间。
- 世界背景、剧情概要、人物小传、正文、写作风格等自然语言长文本默认使用 Element Plus `el-input type="textarea"` 的自适应高度模式；不得使用固定大高度、大 `minRows`、内部滚动条或用户拖拽改变大小作为默认编辑方式。
- 创作长文本默认设置 `resize="none"`，高度应随内容自然向下增长，由页面负责滚动；初始空内容通常只占约 2～4 行，不得为了填满页面预留十几行空白编辑区。
- 创作型长文本优先使用无边框或弱边界的文档式编辑样式，不得默认使用明显的大矩形表单框包围正文内容。短字段仍可使用标准输入框。
- 页面视觉层级应主要依靠标题、间距、字体和分隔关系建立，不得为了区分每个字段而新增大量 Card、边框框体、背景块或自定义容器。
- 一个主要页面应保持单一视觉语言。已有 Element Plus 控件只做必要样式覆盖，不得在同一页面同时维护多套表单视觉体系。

## 模型 Prompt 边界

- System Prompt 和 User Prompt 只允许包含两类信息：
  1. 模型完成当前创作、判断、审核或抽取任务真正需要的业务语义与上下文；
  2. 模型返回结果必须遵守的输出协议、字段 Schema 和字段语义。
- 后端实现细节不得写入 Prompt，包括但不限于：数据库表/字段、Java 类名、VO/DTO/PO、Repository、接口路径、`nodeCode`、`sequenceNo`、数据库 ID、持久化状态、Draft 状态、后端自动分配规则、后端校验规则和工作流内部状态。
- 后端拥有的结构字段应由代码生成、补充和校验。如果字段不属于模型输出 Schema，不得在 Prompt 中通过“不要返回该字段”“该字段由系统生成”等方式向模型解释后端实现。
- Prompt 中不得直接暴露 Java Enum 名称作为创作语义。模型确实需要知道当前任务类型时，应使用“全书大纲”“卷大纲”“单章大纲”“章节计划”等自然语言业务概念。
- 数据库存储形式不得反向决定模型输出形式。例如数据库使用 JSON 字符串保存列表时，模型仍应优先返回真正的数组，由后端负责序列化。
- 判断一项信息是否应该进入 Prompt 时，只问：“模型不知道它，是否会影响本次创作/判断结果？”如果不会，则不应放入 Prompt。

## 模型联调环境

- 本地真实模型接入、启动和联调统一使用 `dev` profile；不为 DeepSeek、OpenAI 或其他模型供应商新增独立 Spring profile。
- 模型供应商、地址、模型名和参数通过 `application-dev.yml` 中的 Spring AI 配置切换，Java Bean 装配不得依赖供应商专属 profile。
- `test` profile 只运行不联网的自动化测试，不调用真实模型、不消耗模型额度。
- Agent 真实模型联调用例统一以 `RealModelIT` 结尾，使用 `dev` profile；用例不对模型内容添加
  Assert 断言，只打印 system prompt、user prompt 和模型返回结果，供人工检查提示词与实际输出。
- 真实模型验证应复用现有章节生成 HTTP 接口；除非业务契约发生变化，不得为了模型连通性重复新增 Controller 或 API 接口。
- API Key 等密钥不得提交到 Git；本地联调时必须通过未跟踪的本地配置或环境变量注入。

## 工作总结规则

- 每完成一段独立工作，都必须更新 `docs/implementation-summary.md`。
- 每条摘要至少说明完成了什么、涉及的主要目录或文件，以及验证结果。
- 摘要应记录已经实现并验证的内容，不要把计划中的内容写成已完成。
- 配置、目录结构、测试迁移、文档和规则调整等独立工作，也都要追加到总结中。
- 禁止直接推送到远程