package cn.ninth.novel.memory.fixture;

/**
 * Candidate semantic types required by the memory foundation fixtures.
 */
public enum FixtureCandidateType {
    EVENT,
    FACT,
    STATE_CHANGE,
    THREAD_CHANGE
}
