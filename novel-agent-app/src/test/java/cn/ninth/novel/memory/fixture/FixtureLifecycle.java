package cn.ninth.novel.memory.fixture;

/**
 * Test-only lifecycle values for canonical fixture records.
 */
public enum FixtureLifecycle {
    ACTIVE,
    RESOLVED,
    ARCHIVED,
    SUPERSEDED,
    INVALIDATED
}
