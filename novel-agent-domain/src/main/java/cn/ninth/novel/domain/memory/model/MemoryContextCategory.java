package cn.ninth.novel.domain.memory.model;

/** ContextPack 的固定类别，不引入额外的图结构或动态类别。 */
public enum MemoryContextCategory {
    RULES,
    OPEN_LOOPS,
    CURRENT_STATES,
    CONSOLIDATED,
    EPISODES
}
